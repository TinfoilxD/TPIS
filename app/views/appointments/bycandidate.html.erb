<p id="notice"><%= notice %></p>

<% @appointments.each do |appointment| %>

<% @id = appointment.application_form_id %>
<% @fapp = ApplicationForm.find(params[:id]) %>
<% @id = @fapp.candidate_id %>
<% @can = Candidate.find(params[:id])%>
<%end%>
<h1>Appointments for <%= @can.full_name %></h1>

<table>
  <thead>
  <tr class="bycandidate">
    <th>Start</th>
    <th>End</th>
    <th>Candidate</th>
    <th>Faculty</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>

  <% @appointments.each do |appointment| %>

  <!--Messy Logic to handle variables.-->
      <% @id = appointment.faculty_id %>
      <% @faculty = Faculty.find(params[:id]) %>
      <% @id = appointment.application_form_id %>
      <% @fapp = ApplicationForm.find(params[:id]) %>
      <% @id = @fapp.candidate_id %>
      <% @can = Candidate.find(params[:id])%>


      <tr>
        <td><%= appointment.start.strftime('%B %e, %H %l:%M  ') %></td>
        <td><%= appointment.end.strftime('%B %e, %H %l:%M') %></td>
        <td><%= @can.full_name%> </td>
        <td><%= @faculty.full_name%></td>
        <td><%= link_to 'Show', appointment , class:'btn btn-default btn-xs'%></td>
        <td><%= link_to 'Edit', edit_appointment_path(appointment) ,class:'btn btn-default btn-xs'%></td>
        <td><%= link_to 'Destroy', appointment, method: :delete, data: { confirm: 'Are you sure?' } ,class:'btn btn-default btn-xs'%></td>
      </tr>


  <% end %><tr>
    <div class="h-divider"></div>
  </tr>
  </tbody>
</table>

<br>

<%= link_to 'Back', url_for(:back), class:'btn btn-default btn-S'  %>
