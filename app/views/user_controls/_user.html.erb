<tr>
  <td><%=user.email%></td>
  <td><%=user.role%></td>


  <td><%= link_to 'Show', admin_user_edit_path(user), class:'btn btn-default btn-xs', method: :get%></td>


  <%if user_signed_in? and user.email != current_user.email then%>
      <td><%= link_to 'Delete', admin_user_delete_path(user), method: :delete, class:'btn btn-danger btn-xs', data: {confirm: 'This will permanently delete an account. Are you sure?'}%></td>
  <%end%>


</tr>

